apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: idp-pg-backstage
  namespace: idp-data
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: idp-pg-backstage
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: idp/postgres/backstage
        property: username
    - secretKey: password
      remoteRef:
        key: idp/postgres/backstage
        property: password
